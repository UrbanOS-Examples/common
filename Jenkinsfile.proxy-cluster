node('master') {
    ansiColor('xterm') {
        stage('Checkout') {
            checkout scm
        }

        stage('Deploy') {
            dir('alm') {
                sh('../scripts/deploy_proxy_cluster.sh')
            }
        }
    }
}