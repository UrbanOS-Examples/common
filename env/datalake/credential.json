{
    "cloudPlatform": "AWS",
    "description": "",
    "name": "cb-credential",
    "parameters": {
        "selector": "role-based",
        "roleArn": "arn:aws:iam::{{ aws_account_number }}:role/{{ cloudbreak_credential }}"
    }
}